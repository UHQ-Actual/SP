{
  "//": "Copy into your agent's MCP config. VS Code / GitHub Copilot: .vscode/mcp.json uses the `servers` key shown here. Claude Desktop uses `mcpServers` instead (same inner shape). Replace the absolute paths, or keep ${workspaceFolder} if your host expands it.",
  "servers": {
    "sharepoint-list": {
      "type": "stdio",
      "command": "python",
      "args": ["${workspaceFolder}/sharepoint-list-agent/server/sharepoint_list_mcp.py"],
      "env": {
        "SPLA_EXPORT_DIR": "${workspaceFolder}/sharepoint-list-agent/config/exports"
      }
    }
  }
}
